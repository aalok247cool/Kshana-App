import 'package:flutter/material.dart';

class KshanaExportsPage extends StatelessWidget {
  const KshanaExportsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Kshana Exports'),
      ),
      body: const Center(
        child: Text('Kshana Exports content goes here!'),
      ),
    );
  }
}